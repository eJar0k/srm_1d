# Combustion gas transport for Risky Batman V3 Jan2024 (Zerox motor).
# RPA-derived FROZEN values. Preserved for diagnostic reference; NOT
# loaded by default as of v0.7.2 cleanup (2026-05-23) — the default
# `zerox.transport.yaml` now ships the effective RPA pair.
#
# v0.7.2 cleanup re-survey is verifying whether the Phase 5 structural
# diagnosis (ignition-kernel artifact, not gas-transport-driven)
# generalizes across the fired-motor library. Load explicitly via
#     run_from_ric(..., transport_path='zerox.frozen.transport.yaml')
#
# Historical note (from the original zerox.transport.yaml comment):
# "Effective values were tested in the zerox_a_sweep workflow and
#  amplified the ignition spike (wrong direction for this motor)
#  without helping the plateau." — v0.6.0-era observation.
mu: 8.604e-5
k: 0.3455
Cp: 2052.0
