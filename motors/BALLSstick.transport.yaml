# Combustion gas transport for UPET (BALLSstick), approx.
# RPA-derived EFFECTIVE values. Loaded automatically by openmotor_adapter
# when reading BALLSstick.ric.
#
# v0.7.2 cleanup (2026-05-23) switched the default from frozen to
# effective. Frozen sibling preserved at
# `BALLSstick.frozen.transport.yaml`.
#
# mu: RPA outputs a single dynamic viscosity for both formulations;
#     matches the frozen sibling.
mu: 9.009e-5
k: 0.5761
Cp: 2629.0
